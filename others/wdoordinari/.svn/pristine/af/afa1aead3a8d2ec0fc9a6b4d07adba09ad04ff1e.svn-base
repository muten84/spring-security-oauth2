/*import { Injectable, EventEmitter } from '@angular/core';

import { Observable, Observer, BehaviorSubject, Subject } from 'rxjs/Rx';
import 'rxjs/add/operator/share';
import { BookingSearchItem } from '../sinottico-prenotazioni/sinottico-prenotazioni-model';
import { BOOKINGS } from './mock/mock-booking-search';
import { RicercaBookingFilter } from '../sinottico-prenotazioni/sinottico-prenotazioni-model'
import { RicercaPrenotazioniTableComponent } from '../sinottico-prenotazioni/sinottico-prenotazioni-table.component'
import { MiddleComponent, HeaderComponent } from '../util/component-interface';

@Injectable()
export class ComponentHolder {

    static _tableComponent: RicercaPrenotazioniTableComponent;
    static _componentRegistry: Map<string, SearchComponent> = new Map<string, SearchComponent>();
    static _headerComponentRegistry: Map<string, HeaderComponent> = new Map<string, HeaderComponent>();



    constructor() {

    }


    public static setMiddleComponent(key: string, comp: SearchComponent) {
        //this._tableComponent = comp;
        this._componentRegistry.set(key, comp);
    }

    public static getMiddleComponent(key: string): MiddleComponent{
        //return this._tableComponent;
        return this._componentRegistry.get(key);
    }

    public static setHeaderComponent(key: string, comp: HeaderComponent) {
        //this._tableComponent = comp;
        this._headerComponentRegistry.set(key, comp);
    }

    public static getHeaderComponent(key: string): HeaderComponent {
        //return this._tableComponent;
        return this._headerComponentRegistry.get(key);
    }
}*/